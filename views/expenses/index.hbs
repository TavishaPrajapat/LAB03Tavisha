<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    
    <!-- Bootstrap CSS -->
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css"
      integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65"
      crossorigin="anonymous"
    />
    <style>
      /* Set background color of the body */
      body {
        background-color: #D4BEE4;
        margin: 0;
        padding: 0;
      }

      /* Jumbotron Header */
      .jumbotron {
        background-color: #8967B3; /* Customize background color */
        color: white;
        top: 0;
        left: 0;
        padding: 40px 20px;
        text-align: center;
        display: flex;
        flex-direction: column;
        justify-content: center;
        height: 200px;
        width: 100%;
      }

      /* Navbar */
      .navbar {
        background-color: #3B1E54 !important;
        position: relative;
        z-index: 2;
      }

      .navbar-nav .nav-link {
        font-size: 16px;
        padding-left: 20px;
        padding-right: 20px;
      }

      .navbar .container-fluid {
        display: flex;
        justify-content: space-between;
        align-items: center;
      }
          /* Styling for the Transaction Tracker header */
    h1 {
      font-size: 3rem; /* Adjust the font size */
      font-weight: bold; /* Make the header bold */
      text-align: center; /* Center align the header */
      color: #3B1E54; /* Set a deep color for the header */
      margin-top: 50px; /* Add top margin for spacing */
    }

    /* Styling for the paragraph description */
    p {
      font-size: 1.2rem; /* Set the font size */
      color: #333; /* Dark grey color for better readability */
      text-align: center; /* Center align the paragraph */
      max-width: 800px; /* Limit the width for readability */
      margin: 20px auto; /* Center the paragraph and add margin for spacing */
      line-height: 1.6; /* Increase line height for better readability */
      padding: 0 15px; /* Add padding for responsive design */
    }
      /* Button styling */
      .btn-purple {
        background-color: #3B1E54;
        color: white;
        border: none;
        padding: 10px 20px;
        margin: 5px;
        font-size: 16px;
        border-radius: 5px;
        text-decoration: none;
      }

      .btn-purple:hover {
        background-color: #5c2a75; /* Slightly lighter shade on hover */
        color: white;
      }

      /* Table styling */
      table {
        width: 100%;
        border-collapse: collapse;
        margin-top: 30px;
      }

      th, td {
        text-align: center;
        padding: 12px;
        border: 1px solid #ddd;
      }

      thead{
        background-color: #3B1E54;
        color: white;
      }

      tr:nth-child(even) {
        background-color: #f2f2f2;
      }

      tr:hover {
        background-color: #ddd;
      }

      /* Add space between the table and buttons */
      .button-container {
        margin-top: 20px;
        text-align: center;
      }
    </style>
  </head>
  <body>
    
    <!-- Main Content -->
    <div class="container">
      <h1>{{title}}</h1>

      <!-- Add Expense Category button (only visible to authenticated users) -->
      {{#if user}}
        <a href="/expenses/add" class="btn-purple">Add a new Expense Category</a>
      {{/if}}

      <!-- Table to display expense categories -->
      <table class="table table-striped table-hover">
        <thead>
          <tr>
            <th>Category Name</th>
            <th>Description</th>
          </tr>
        </thead>
        <tbody>
          {{#each dataset}}
            <tr>
              <td>{{this.name}}</td>
              <td>{{this.description}}</td>
            </tr>
          {{/each}}
        </tbody>
      </table>
    </div>

    <!-- Bootstrap JS -->
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.min.js"
      integrity="sha384-cuYeSxntonz0PPNlHhBs68uyIAVpIIOZZ5JqeqvYYIcEL727kskC66kF92t6Xl2V"
      crossorigin="anonymous"
    ></script>

    <script src="/javascripts/scripts.js"></script>
  </body>
</html>
